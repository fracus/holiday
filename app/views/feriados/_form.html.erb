<%= form_for(@feriado) do |f| %>
  <% if @feriado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feriado.errors.count, "error") %> prohibited this feriado from being saved:</h2>

      <ul>
      <% @feriado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :fecha %><br />
    <%= f.date_select :fecha %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
